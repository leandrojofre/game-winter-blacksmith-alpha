function keyDown(event) {
	let key = event.key.toLowerCase();

	switch (key) {
		default:
			break;
	}
}

function keyUp(event) {
	let key = event.key.toLowerCase();

	switch (key) {
		default:
			break;
	}
}
